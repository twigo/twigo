# @testing-library/jest-dom/vitest does a bare `import 'vitest'` resolved from
# its hoisted location, so vitest must be reachable from the root node_modules.
public-hoist-pattern[]=*vitest*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
